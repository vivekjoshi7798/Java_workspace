Show an example of Given a list of employees (name, designation)
 convert it to a map<designation, count> using java stream api
 
 
 
 List<Employees> employeeList;
 
Map<String,long> countMap =	employeeList.stream().map(Employee::Designation)
		.collect(Collectors.groupBy(Function.identity(), Collectrs.counting());
		
		
		
list of elements {1,2,3,4,6,8,5,7,9,0,11}
output {11,0,9,1,8,2,7,3,6,4,5}



int[] arr = {1,2,3,4,6,8,5,7,9,0,11};

Arrays.sort(arr);
,2,3,4,5,6,7,8
0,1,9,11

List<Integer> finalList = new ArrayList<>();
int firstIndex = 0;
int LastIndex = arr.lenght;

for(int i=0;i<lenght;i++){

	if(lastIndex!=firstIndex)

	finalList.add(arr[firstIndex]);
	finalList.add(arr[lastIndex]);

	firstIndex ++;
	lastIndex --;	
	
}

for(integer value: finalList){
	
	System.out.print(value
}

		
		
		
		
Employee table
(empid, empName, empsalary, ManagerId)

employees earning more salary there manager



Select e1.empid, e1.emp_name,e1.empSalary,e1.managerId
from Employee e1
join Employee e2 on e1.managerId= e2.managerId
where e1.salary>e2.salary

select distinct Salary
from Employee orderby Salary desc
Limit 1 offset 3;


		
		
get  and load
wait and join
		
		