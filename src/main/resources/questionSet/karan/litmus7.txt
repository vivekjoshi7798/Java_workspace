List<String> abc =Arrays.asList("Karan", "Arjun", "Suman", "Milan"};

List<String> final = abc.stream().filter(str-> str.startswith('K') && str.length > 5).collect(Collectors.toList());


int[] nums = {-2, 1, -3, 4, -1, 2, 1, -5, 4};

cascading
hiburnate sesion
get and put in hibernate session
stack and heap memory
profiles 
actuator
hibernate fetching mechanishm
design pattern
factory
predicate
consumer 
supplier


