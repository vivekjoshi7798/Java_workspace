How do you get distinct characters and their count in a string in Java?


String str = "KaranKumar";

Map<String,Long> countMap = Arrays.stream(str.split("")).distinct()
		.collect(Collectors.groupingBy(Function.identity(), Collectors.counting()));
		
		
		
		Write a Java program to show a NullPointerException.
		
		
interface Sum{

	public void print sum(int a, int b);
}


Sum sumobj = new Sum ((a,b)-> 
								System.out.println(a+b);
							);
							
							
import java.util.*;

public class EmployeeManager {

private HashMap<String,String> employees = new HashMap<>();

public boolean addEmployee(String id, String name) {

	if (employees.containsKey(id)){
	
		System.out.println("Employee already exists!");
		return false;
	}
	else{
	employees.put(id, name);
	return true;
	}
}

public void displayEmployee() {

	for (String key : employees.keySet()) {
	System.out.println("Employee ID: " + key + ", Name: " + employees.get(key));
	}
}

	public static void main(String[] args) {
		EmployeeManager manager = new EmployeeManager();
		manager.addEmployee("1", "John Doe");
		manager.displayEmployee();
	}
}