Id 
name 
age 
city

Employeee table


	Alter table Employeee remove city
	
	
	
	From list of employee get the name of employees having maximum salary and whose age less than 30 and from pune city using java stream
	
	List<Employee> emplist = new ArrayList<>();
	
	List<Employee> emplist2 = emplist.stream().filter(e->e.age < 30 && e.city.equals("pune")).max(e->e.salary)
	
	
	
	string str =  "aaaabbbbbbeeedddddddddaaa“
	
	HashMap<String, Integer> hash = new HashMap<>();
	
	 hash =  Arrays.stream(str.split("")).collect(Collectors.groupby(Function.get(),Collections.counting()));				
	 
	 a:7 
	 
	
	for(Map<String,Integer> entry : hash.getEntrySet()){
	
	String key = entry.getKey();
	Integer Value = entry.getValue();
	
	System.out.print(key+""+value);
	
	}
	
 
Desired output is “a7b6e3d9”




how to remove column
how to add column
delete drop truncate
transient
syncronized
Actuator
how to specifiy which DB to use when we have more than 1 db
how to specify the Qa and PP in the spring boot application
exception handling in spring booot
@requestParam @pathVariable
spring batch
kafka
Native query vs Named query
