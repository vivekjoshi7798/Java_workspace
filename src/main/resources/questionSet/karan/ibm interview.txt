[]


List<Integer> inputList = Arrays.asList(1,4,6,8,23,9,4,3,33);

List<Integer> finalList = inputList.stream().filter(a -> a/2 == 0).boxed()
					.sorted(Comparator.reverseOrder()).collect(Collectors.toList());
					
					
					
Select Max(Salary) from Employee
where Limit 4 offset 1


find a list with duplicates of both

List<Integer> list1 = Arrays.asList(1,4,7,5,8,9);
List<Integer> list2 = Arrays.asList(3,4,7,8,9,10);

List<Integer> finalList = new ArrayList<>();

for(Integer abc : list1){
	finalList.add(abc);
}
for(Integer bcd : list2){
	finalList.add(bcd);
}


Map<Integer,long> finalMap = finalList.stream().collect(Collectors
			.groupingBy(Function.identity(), Collectors.counting()));
			
			4 : 2
			7  : 2
			
for(Map.Entry<Integer,long> entry : finalMap.EntrySet()){
	
	if(entry.getValue().getIntValue() > 1){
		
		System.out.print(entry.getKey()+ ", ");
	}
}






hashmap
@value
circuit Breaker
linkedlist middle element
LinkedList<String> abc= new LinkedList<>();

a->b->c-d-E-f

bst

optional class
service discovery


Kafka
junit